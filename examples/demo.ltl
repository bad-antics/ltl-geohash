// examples/demo.ltl — runnable demo for ltl_geohash

import std::io
use ltl_geohash as lib

fn main() {
    io::println("ltl_geohash demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}